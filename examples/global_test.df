count = 0
memo: dict[int, int] = {}

func fib(n: int) -> int:
    global count, memo
    count += 1
    if n in memo:
        return memo[n]
    if n <= 1:
        return n
    result = fib(n - 1) + fib(n - 2)
    memo[n] = result
    return result

func main():
    print(fib(10))
    print(count)
